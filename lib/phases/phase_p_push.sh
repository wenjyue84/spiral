#!/usr/bin/env bash
# Phase P: Push — optional git push after validation passes
# Currently a no-op; controlled by SPIRAL_AUTO_PUSH_TAGS

run_phase_push() {
  # Future: auto-push to remote when configured
  # For now, pushing is handled by the tag system in spiral_helpers.sh
  :
}
