# SPIRAL 最近10个改进 — 2026-04-12

> 自动生成：SPIRAL 每完成10个故事后更新此文件。

---

**1. US-1219 — 学习分析：为什么有些任务一次就成功，有些要反复重试**
系统现在会自动分析：哪些类型的任务总是一次通过，哪些总要重试很多次。把这些规律总结出来，下次遇到类似任务就能提前做好准备，避免浪费时间反复尝试。

**2. US-1218 — 测试失败追责：精准定位是哪个改动搞坏了测试**
以前测试挂了只知道"有东西坏了"，不知道是哪次改动导致的。现在系统会对比改动前后的测试结果，直接告诉你"是这个故事的代码把这个测试搞坏了"，方便快速修复。

**3. US-1217 — 识别不稳定测试，避免白做工**
有些测试时好时坏（"flaky test"），不是代码的问题。系统现在会追踪测试历史，如果一个测试在最近5轮里有一半时间是坏的，就不会因为它生成新的修复任务，避免浪费精力去修不该修的东西。

**4. US-1216 — 给研究发现的任务打分：哪些研究靠谱，哪些不靠谱**
系统会追踪：通过网络研究发现的任务，最终实现的成功率怎样。如果某类研究总是产出失败的任务，以后就会降低它的权重，让研究方向更精准。

**5. US-1215 — 新任务必须符合项目规则才能被提出**
以前AI提出的新任务有时会违反项目的基本原则。现在每个新任务提出前都会和"项目宪法"对照打分，不合格的直接淘汰，保证每个任务都是合理的。

**6. US-1213 — AI 从历史经验中学习提出更好的任务（ShinkaEvolve 启发）**
系统会读取以前成功实现的经验总结，自动把有用的技巧加入到"提出新任务"的提示词里。以前每轮都是从零开始想，现在会越来越聪明。

**7. US-1214 — 实现代码的AI也能学习历史经验（ShinkaEvolve 启发）**
不只是"想任务"会学习，"写代码"的AI也会了。实现新功能时，系统会自动注入相关的历史经验，比如"上次类似的任务用了这个方法成功了"。

**8. US-1212 — 按依赖顺序合并任务，先打地基再盖楼**
系统在合并新任务时会按照依赖关系排序：如果任务B依赖任务A，就先合并A。这样避免"楼还没打地基就开始装修"的尴尬。

**9. US-1211 — 多项目版本日志生成测试**
保护多项目联合开发时的版本日志功能。确保每个子项目都能正确生成自己的变更记录，发版时不会遗漏任何改动。

**10. US-1210 — 多项目循环依赖检测测试**
保护循环依赖检测功能：如果项目A依赖B、B依赖C、C又依赖A，系统能发现这种死循环并报警，避免卡在无法解决的依赖链里。

---

**本轮总结：** 这10个改进集中在两个方向：(1) **让SPIRAL更聪明** — 从历史经验学习、智能评分、精准追责；(2) **多项目支持更稳固** — 依赖排序、循环检测、版本日志都有测试保护。

**累计进度：** 90/100 (90%) — 本次运行已完成 42 个故事（从48个开始）

---

# SPIRAL 最近10个改进 — 2026-04-12 (第100个故事里程碑!)

> 自动生成：SPIRAL 达到100个已通过故事。

---

**1. US-1231 — Worker 卡死自动检测和重启**
如果某个 Worker 长时间没有心跳信号（说明它卡住或崩溃了），系统会自动检测到并重启它，不需要人工干预。保证长时间运行时不会因为某个 Worker 挂了而卡住整个流程。

**2. US-1229 — 仪表盘：Worker 工作时间线图**
在仪表盘上新增了"泳道图"，可以直观看到每个 Worker 在什么时间段处理了哪个任务、每个阶段花了多长时间。方便发现哪个 Worker 比较慢、哪个阶段是瓶颈。

**3. US-1228 — 研究阶段重复查询检测性能测试**
保护研究阶段的"语义查重"功能——如果系统已经研究过类似的问题，就不再重复花钱去查。这个测试确保查重功能又快又准。

**4. US-1227 — 经验记忆库：存储、检索、注入历史经验**
系统现在有一个"经验数据库"，能把每次成功或失败的实现经验存起来，下次遇到类似任务时自动调出来参考。像一个有记忆的工程师，不会重复犯同样的错误。

**5. US-1226 — 仪表盘：任务重试耗尽分析器**
在仪表盘上新增了"重试耗尽分析"视图，能看到哪些任务反复失败最终被放弃了，以及失败的原因分类。帮你发现系统的薄弱环节。

**6. US-1225 — 多项目文件冲突检测测试**
保护跨项目文件冲突检测功能。当两个子项目修改了同一个文件，系统能发现冲突并可视化展示，避免代码互相覆盖。

**7. US-1224 — 大任务自动拆分测试**
保护"自动拆分"功能——当一个任务太大太复杂反复失败时，系统会自动把它拆成几个小任务分别完成。这个测试确保拆分逻辑正确。

**8. US-1223 — 验收标准自动执行器测试**
保护"自动检查验收标准"功能。每个任务都有验收条件，系统能自动解析这些条件并检查代码是否满足，不再纯靠人工判断。

**9. US-1222 — 验收标准解析器测试**
保护把文字描述的验收标准转换成可执行检查的功能。比如"API 响应时间 < 200ms"会被解析成实际的性能测试。

**10. US-1220 — 部分成功也能保存（部分胜利提交）**
以前如果一个任务5个验收标准只通过了3个，整个实现都会被丢弃。现在系统会保存已通过的部分，下次只需要修复剩下的2个，不用从头来过。

---

**本轮总结：** 这10个改进让SPIRAL更"聪明"和"记忆力更好"：(1) **自我修复能力** — Worker 卡死自动重启、大任务自动拆分、部分成功保存；(2) **可观测性** — 仪表盘泳道图、重试分析、冲突可视化；(3) **经验积累** — 经验数据库让系统越跑越聪明。

**累计进度：** 100/112 (89.3%) — 本次运行已完成 52 个故事（从48个开始），发现了64个新故事

---

# SPIRAL 最近10个改进 — 2026-04-12 (第110个故事里程碑)

> 自动生成：SPIRAL 达到110+个已通过故事。

---

**1. US-1261 — 多项目智能去重合并**
新增了一个"命名空间感知"的合并工具。当多个子项目同时提交故事时，系统能根据项目前缀（如 PROJ1/US-100）来判断哪些是重复的、哪些不是。避免不同项目的同名故事被误判为重复而丢弃。

**2. US-1243 — 根据历史数据优化AI模型选择的性价比**
保护"模型性价比校准"功能。系统会分析历史记录：用便宜的模型（haiku）成功了多少次、用贵的模型（opus）成功了多少次，算出每种模型"每次成功的平均花费"，自动选最划算的。

**3. US-1242 — 防止提示词太长导致AI崩溃**
保护"上下文窗口溢出保护"功能。当要发给AI的提示词太长（超过模型能处理的范围），系统会自动裁剪，优先保留最重要的部分，而不是直接崩溃报错。

**4. US-1241 — 智能分类失败原因，对症下药**
保护"失败分类器"功能。以前任务失败了只知道"失败了"，现在系统能自动分类：是语法错误、超时、还是逻辑错误？不同类型的失败用不同的策略重试，不再一刀切。

**5. US-1240 — 防崩溃日志：保护任务清单不被写坏**
保护"事务日志"功能。每次修改任务清单（prd.json）前都先写一条日志，如果中途崩溃，下次启动时能从日志恢复，不会丢失数据或产生写了一半的脏数据。

**6. US-1239 — 从历史数据预测任务速度和成本**
保护"速度预测模型"功能。系统会从历史数据中学习：每种类型的任务平均要花多少时间和钱，用来更准确地估算剩余工作量和预算。

**7. US-1238 — 全流程性能压测**
新增了一个端到端性能测试，模拟完整的 SPIRAL 循环（从研究到完成），测量每个阶段花了多少时间，确保系统整体性能达标。

**8. US-1252 — 安全测试：防止AI处理提示词时泄露敏感信息**
保护提示词处理模块的安全性。确保在裁剪和估算提示词长度时，不会意外暴露 API 密钥、令牌等敏感内容。

**9. US-1251 — 全流程状态机测试**
新增了一个端到端测试，验证 SPIRAL 的每个阶段是否按正确顺序执行，每个故事的状态是否正确流转（从"待办"到"进行中"到"完成"）。

**10. US-1250 — 端到端测试基础设施**
搭建了完整的端到端测试框架，包括模拟环境、测试夹具、临时目录管理。以后写新的端到端测试更快更方便，不用每次都从零搭环境。

---

**本轮总结：** 这10个改进集中在三个方向：(1) **安全和稳定** — 防崩溃日志、溢出保护、安全测试；(2) **智能决策** — 失败分类、性价比模型选择、速度预测；(3) **测试基础设施** — 端到端测试框架让质量保障更系统化。

**累计进度：** 116/130 (89.2%) — 本次运行已完成 68 个故事（从48个开始），发现了82个新故事

# SPIRAL 最近10个改进 — 2026-04-14 (第130个故事里程碑)

> 自动生成：SPIRAL 每完成10个故事后更新此文件。

---

**1. US-1172 — E2E集成测试：联邦式多子项目完整循环验证**
这个测试模拟了多个子项目合并运行的完整流程，确保SPIRAL能同时管理多个项目的故事而不出现混乱。就像是给整个系统做了一次全面的"体检"，保证各部分协调工作。

**2. US-1166 — 故事验证信心评分器：让好故事优先处理**
系统现在能给每个待实现的故事打一个"把握分数"——越有把握实现的故事排在越前面。这样SPIRAL就不会在低质量的故事上浪费时间，先完成那些有清晰目标的任务。

**3. US-1170 — Ralph实现记忆注入：从历史成功案例中学习**
当Ralph要实现一个新功能时，系统会自动查找过去类似任务的成功经验，把它们注入到提示词里。就像老工程师把经验传授给新人，让每次实现都站在前人的肩膀上。

**4. US-1168 — 跨项目文件冲突检测：防止不同子项目踩踏同一文件**
在多项目并行开发时，如果两个子项目都修改了同一个文件，系统现在能及时发现并生成冲突报告。避免了"各自为政"导致的代码互相覆盖问题。

**5. US-1249 — 分析仪表盘：重试历史表格新增筛选控件**
在分析页面的"重试历史"表格里，现在可以按故事ID、状态、模型等条件筛选，不再需要翻遍所有记录才能找到想看的数据。一个小按钮，省了很多时间。

**6. US-1267 — 清理死代码：移除未使用的变量**
检查并删除了代码里多个"僵尸变量"——定义了但从未被用到的变量。保持代码整洁，减少误解风险，也让代码分析工具不再报警。

**7. US-1167 — 架构约束验证器：确保代码修改符合设计原则**
增加了一个自动检查器，在每次代码通过测试后验证改动有没有违反项目的架构规则（比如某些模块不能互相依赖）。在问题变大之前就把它们挡在门外。

**8. US-1272 — 修复CI流水线：移除损坏子模块并更新CodeQL**
CI/CD自动化测试流水线之前因为一个损坏的子模块而卡住，代码质量检查和安全扫描都跑不起来。修复后，每次提交都能正常触发完整的安全和质量检查。

**9. US-1254 — 仪表盘新API：实时显示每小时完成故事数**
在仪表盘新增了一个接口，可以查询SPIRAL最近每小时完成了多少个故事。让你不用打开日志文件就能直观感受系统当前的工作速度。

**10. US-1247 — 文件锁保护：防止事件日志同时写入损坏**
当多个工作进程同时往同一个日志文件写数据时，以前可能导致文件内容混乱。现在加了文件锁，保证每次只有一个进程在写，数据不再丢失或错乱。

---

**本轮总结：** 这10个改进集中在两个方向：(1) **质量保障更全面** — 从E2E集成测试、架构约束验证、到CI流水线修复，各层面的质量防线都得到了加固；(2) **系统更聪明、更可观测** — 历史经验注入让实现更有把握，仪表盘新增筛选和吞吐量API让运行状态一目了然。

**累计进度：** 137/146 (93.8%) — 本次运行新增完成了 12 个故事

---

# SPIRAL 最近10个改进 — 2026-04-14 (第140个故事里程碑)

> 自动生成：SPIRAL 每完成10个故事后更新此文件。

---

**1. US-1172 — E2E集成测试：联邦多项目完整循环压力验证**
模拟多个子项目合并运行的完整SPIRAL循环，确保系统在管理多个项目故事时不会互相干扰。这相当于给整个协调系统做了一次真实场景的全面压力测试，发现隐藏问题。

**2. US-1282 — 故事详情面板：实现尝试历史泳道图**
在点击查看某个故事的详情时，现在顶部会出现一排彩色横条，直观展示这个故事经历了几次尝试、每次花了多长时间、最终是成功还是失败。一眼就能看出故事的"磨难史"。

**3. US-1166 — 故事验证信心评分：让高质量故事优先实现**
给每个待实现的故事打一个0到100的"信心分"，分数高的说明验收标准清晰、实现把握大。SPIRAL据此优先处理最有把握的故事，避免在模糊需求上反复失败。

**4. US-1170 — Ralph历史经验注入：站在成功案例的肩膀上**
实现新功能前，系统自动从历史数据库里找出最相似的成功案例，把关键经验注入到给Ralph的提示里。让每次实现都能借鉴过去的成功，而不是重复走弯路。

**5. US-1281 — 分析页面：迭代范围筛选药丸按钮**
在分析仪表盘顶部新增了"全部 / 最近10轮 / 最近20轮 / 最近50轮"快速切换按钮。一键就能聚焦查看最近的数据，不需要在海量历史数据里费力分辨。

**6. US-1168 — 跨项目文件冲突检测报告**
当多个子项目的故事都修改同一个文件时，系统会检测到冲突并生成可视化报告，告诉你哪些文件被哪几个项目同时触碰。防止并行开发时代码相互覆盖的问题。

**7. US-1249 — 分析重试历史表格新增筛选功能**
在分析页面的重试历史表格里，现在可以按故事ID、状态或模型类型快速筛选记录，不再需要逐行翻查。极大提升了在大量历史记录里定位特定故事的效率。

**8. US-1274 — 分析页面：模型费用占比甜甜圈图**
新增了一个可视化图表，以甜甜圈形式展示使用不同AI模型（haiku、sonnet、opus）的费用占比。一眼看出哪个模型花了最多钱，方便优化成本。

**9. US-1267 — 清理无用代码：移除僵尸变量**
扫描并清除了代码中多处"声明了但从未使用"的变量，消除代码分析工具的警告。代码越干净，越容易维护，出Bug的概率也越低。

**10. US-1167 — 架构约束验证器：防止设计原则被悄悄违反**
每次代码通过测试后，系统会自动检查改动有没有触犯项目的架构禁区（比如循环依赖、模块越权访问）。把架构问题扼杀在萌芽阶段，而不是等到积重难返。

---

**本轮总结：** 这10个改进分三个主题：(1) **分析仪表盘全面升级** — 甜甜圈图、范围筛选、重试表格筛选、故事尝试泳道图，让数据可视化更丰富直观；(2) **系统更聪明** — 历史经验注入、信心评分让实现成功率更高；(3) **工程质量提升** — 架构验证、冲突检测、僵尸代码清理，从多角度守护代码健康。

**累计进度：** 140/146 (95.9%) — 本次运行已完成 15 个故事（从125开始）

---

# SPIRAL 最近10个改进 — 2026-04-14 (第10个故事里程碑)

> 自动生成：SPIRAL 每完成10个故事后更新此文件。

---

**1. US-1209 — 依赖追踪回归测试**
为 CLI 命令 `spiral trace-dependencies` 添加回归测试，确保跨项目依赖解析不会因未来改动而悄悄失效。这是质量防护网，让开发者改代码时更有信心。

**2. US-1210 — 全流程端到端集成测试**
创建了一个模拟完整 SPIRAL 循环（A→R→T→S→E→M→X→G→I→V→C）的端到端测试，用假 Claude CLI 替代真实 API 调用。这让系统在不花费 API 费用的情况下验证整体流程是否正常。

**3. US-1211 — 重试升级与崩溃恢复集成测试**
扩展端到端测试套件，覆盖两个关键场景：故事失败时模型自动从 haiku 升级到 sonnet，以及工作进程崩溃后的恢复机制。这让 SPIRAL 的容错能力有了可验证的保障。

**4. US-1212 — 联邦 PRD：跨子项目故事合并**
支持在 `prd.json` 中用 `prd.include` 引用子项目，Phase M 合并时自动加上命名空间前缀（如 `web:US-101`）。大型团队可以将前后端故事分开管理，再统一调度实现。

**5. US-1213 — `--config` 标志支持多配置文件**
在 `spiral.sh` 中新增 `--config` 参数，允许加载自定义配置文件覆盖默认设置。开发者可以为测试、生产、演示等不同场景各备一份配置，无需每次手动修改。

**6. US-1214 — spiral-ui 后端配置 API**
创建 `lib/spiral_ui_config.py` 模块，负责加载、验证并持久化运行时配置（如最大待办故事数、模型路由策略）。这是仪表盘设置面板的数据层基础。

**7. US-1215 — spiral-ui 设置选项卡**
在仪表盘中新增「设置」选项卡，可直接查看和编辑核心配置项，无需手动修改配置文件。改动后实时保存，让非技术用户也能调整 SPIRAL 行为。

**8. US-1216 — Phase V 精细化验证：测试标记与文件触碰断言**
Phase V 现在能解析代码中的测试标记（如 `// STORY:US-1216`），并核实故事声明的文件是否真的被创建或修改。验证结果更精确，误报更少。

**9. US-1217 — Activity Log 数据层**
创建 `useStoryTimeline` Hook，将 `results.tsv` 解析为带类型的故事时间线数组，并在仪表盘注册新的「活动日志」选项卡路由。这是活动日志功能的数据基础。

**10. US-1218 — Activity Log 表格组件**
构建 `ActivityLogTable` 组件，展示可排序的故事执行记录，带彩色状态徽章、按状态筛选功能和故事详情弹窗。让用户轻松查看每个故事的实现历史。

---

**本轮总结：** 这10个改进分三个主题：(1) **测试覆盖全面补强** — 端到端测试、回归测试、崩溃恢复测试，让系统稳定性可验证；(2) **配置管理现代化** — `--config` 标志、配置 API、设置选项卡，告别手动改文件；(3) **可观测性升级** — Activity Log 数据层与表格组件，让每个故事的执行历史一目了然。

**累计进度：** 20/30 (66.7%) — 本次运行已完成 10 个故事

---

# SPIRAL 最近10个改进 — 2026-04-14 (第20个故事里程碑)

> 自动生成：SPIRAL 每完成10个故事后更新此文件。

---

**1. US-1219 — CLI 配置管理子命令**
新增 `spiral config list` 和 `spiral config set KEY VALUE` 两个子命令，让用户无需打开配置文件即可查看和修改运行时配置。这让命令行操作更流畅，也方便脚本自动化调整设置。

**2. US-1220 — 项目章程解析模块**
创建 `lib/spiral/constitution_parser.py`，将 `constitution.md` 解析为结构化字典（目标→聚焦规则）。这是后续自动合规检查的基础，让 SPIRAL 能"读懂"自己的行为准则。

**3. US-1225 — 元引导层：成功模式库**
每当故事零重试直接通过时，自动提取其特征（文件数、验收标准数、关键词等）并记录到 `_success_patterns.jsonl`。Phase A 故事生成时读取这份历史，优先推荐与过往成功案例相似的故事类型。

**4. US-1226 — 元引导层：循环质量评分**
每次迭代结束后计算一个综合健康分（0–100），由三个子指标组成：首次通过率、候选重复比、复杂度预测准确率。让团队一眼看出 SPIRAL 这轮"运转得好不好"。

**5. US-1227 — 元引导层：验收标准质量门**
Phase S 验证时，自动对每个故事的验收标准打分：包含可度量断言得分高，使用模糊措辞（"正常工作"、"符合预期"）得分低。评分低于阈值的故事会收到改进提示，减少模糊需求流入实现阶段。

**6. US-1231 — CHANGELOG 生成模块**
创建 `lib/changelog_generator.py`，调用 `git-cliff` 工具，根据 Git 提交历史和 `prd.json` 自动生成 `CHANGELOG.md`。告别手写更新日志，版本记录从此自动化。

**7. US-1232 — Phase G：自动生成并提交变更日志**
扩展 Phase G，在每次迭代完成后自动调用 `changelog_generator.py` 并将 `CHANGELOG.md` 提交到代码库。每次迭代都留下一份结构化的变更记录，代码历史更清晰。

**8. US-1233 — spiral-ui 后端：工作进程列表与日志流**
新增两个 API：`GET /api/workers/list` 扫描活跃工作进程并返回状态，SSE 端点实时推送指定进程的日志流。这是仪表盘「工作进程」选项卡的后端支撑。

**9. US-1234 — spiral-ui 前端：工作进程实时监控选项卡**
在仪表盘新增「Workers」选项卡，每2秒轮询进程状态并展示进程表格，点击任意进程可实时查看其日志输出。让多工作进程并行时的状态透明可见。

**10. US-1241 — spiral-ui 仪表盘：实时代币消耗折线图**
在仪表盘新增实时折线图，展示累计代币消耗和预测总费用随时间的变化趋势。数据通过 WebSocket 从 `results.tsv` 流式更新，让成本控制更直观。

---

**本轮总结：** 这10个改进分三个主题：(1) **元引导层初步落地** — 成功模式库、循环质量评分、验收标准质量门，让 SPIRAL 开始自我优化循环质量；(2) **变更日志自动化** — changelog 生成模块与 Phase G 集成，版本记录从此全自动；(3) **工作进程可视化** — Workers 选项卡与实时代币图表，多进程并行时状态一览无余。

**累计进度：** 20/30 (66.7%) — 本次运行已完成 20 个故事

---

# SPIRAL 最近10个改进 — 2026-04-14 (第30个故事里程碑)

> 自动生成：SPIRAL 每完成10个故事后更新此文件。

---

**1. US-1229 — 元引导层：目标覆盖追踪与停滞检测**
自动测量 prd.json 的 `goals[]` 目标数组被已通过故事覆盖的比例。如果某个目标连续5次迭代没有任何进展，系统会在终端发出警告。让 SPIRAL 始终朝着真正重要的目标推进，避免"在细枝末节上反复打转"。

**2. US-1231 — 自动变更日志生成**
创建 `changelog_generator.py`，调用 `git-cliff` 工具，根据 Git 提交历史和 prd.json 自动生成结构化 CHANGELOG.md。每次迭代结束后，版本记录自动更新，告别手写更新日志。

**3. US-1232 — Phase G：迭代后自动提交变更日志**
扩展 Phase G，在每次迭代所有故事完成后自动生成并提交 CHANGELOG.md。版本历史从此完全自动化，代码库始终保有最新的变更记录。

**4. US-1233 — spiral-ui 后端：工作进程列表与实时日志流**
新增 `GET /api/workers/list` 端点，扫描活跃工作进程并返回状态信息；SSE 端点实时推送指定进程的日志输出。这是仪表盘「Workers」选项卡的后端数据基础。

**5. US-1234 — spiral-ui 前端：工作进程实时监控选项卡**
在仪表盘新增「Workers」选项卡，每2秒自动刷新进程状态，展示实时状态表格，点击任意进程可查看滚动日志。多进程并行时，谁在运行、谁卡住了，一目了然。

**6. US-1241 — spiral-ui 仪表盘：实时代币消耗折线图**
在仪表盘新增实时折线图，展示累计代币消耗和预测总费用的时间曲线。数据通过 WebSocket 从 `results.tsv` 流式更新。成本趋势可视化，帮助用户及时发现异常消耗。

**7. US-1243 — Phase V：文件存在性验证合约**
Phase V 现在支持验证故事声明的文件是否真的被创建或修改，并支持文件内容的正则匹配断言。测试驱动故事的验收从"猜测式"升级为"有合约保障"。

**8. US-1250 — spiral-ui 配置 API 安全测试**
为 POST `/api/config` 端点编写安全测试，验证未认证请求会被拒绝、敏感配置项不会泄露。SPIRAL 的 Web API 层安全性从此有了自动化验证。

**9. US-1251 — Phase V：支持 `@spiral:verify` 测试标记**
Phase V 现在能识别测试代码中的 `@spiral:verify(story-id)` 注解，精准执行与指定故事关联的测试函数。每个故事的验收验证更精确，误报更少，信噪比更高。

**10. US-1255 — Phase I：超时处理与优雅范围缩减**
为 Ralph 实现环节新增5分钟超时保护。首次超时时，`scope_reducer.py` 自动精简可选验收标准后重试，避免整个故事因复杂度过高而直接失败。这让 SPIRAL 在面对大型故事时更有弹性。

---

**本轮总结：** 这10个改进分三个主题：(1) **元引导层完善** — 目标覆盖追踪与停滞告警，让 SPIRAL 的自我优化闭环更完整；(2) **可观测性全面升级** — Workers 监控、代币折线图、变更日志自动化，让系统运行状态透明可见；(3) **验证与韧性加强** — 文件合约验证、测试标记精准匹配、超时优雅降级，让每个故事的实现质量更有保障。

**累计进度：** 30/40 (75.0%) — 本次运行共完成 30 个故事

---

# SPIRAL 最近10个改进 — 2026-04-27 (第50个故事里程碑)

> 自动生成：SPIRAL 每完成10个故事后更新此文件。

---

**1. US-1212 — Federated PRD：新增 prd.include 字段以组合子项目故事**
让 prd.json 可以通过 `include` 数组引入子项目的故事文件，Phase M 合并时自动加上命名空间前缀（如 `web:US-100`）并检测循环引用。这使 SPIRAL 可以管理多仓库、多团队的复杂项目，把分散的需求统一到一个调度循环里。

**2. US-1210 — 新增端到端集成测试：用模拟 Claude CLI 跑完整 SPIRAL 循环**
在 `tests/test_spiral_e2e_integration.py` 增加一个测试，用 monkeypatch 把 Claude CLI 调用全部替换成假返回，从而完整跑一遍 A→R→T→S→E→M→X→G→I→V→C 阶段链。它能验证阶段执行顺序和最终 prd.json 状态，且零 API 成本，是 CI 的稳定保障。

**3. US-1219 — CLI 新增 `spiral config list` 与 `spiral config set` 指令**
不需要再手动编辑 spiral.config.sh，直接命令行就能列出当前配置或修改某个键值。改动会原地更新到 spiral.config.sh，下次运行立即生效。这显著降低了用户调整运行参数的门槛。

**4. US-1217 — spiral-ui 新增 useStoryTimeline Hook 与 Activity Log 标签**
为新的"活动日志"页打基础：解析 results.tsv 成带类型的故事时间线数组，并在 App.tsx 注册新路由。页面先显示加载/空状态占位符，下一个故事再把表格组件接上去。

**5. US-1211 — 新增端到端测试：故事重试模型升级与 Worker 崩溃恢复**
两个新测试：一个模拟故事失败、验证模型从 haiku 升级到 sonnet 并写入 results.tsv；另一个模拟 worker 崩溃、验证主循环会重启 worker 并完成剩余故事。这把"失败应对"这个最难测的路径纳入了自动化保护。

**6. US-1282 — [安全测试] spiral-ui Settings 标签的鉴权与权限保护**
新增安全测试，验证 SettingsTab React 组件及其后端 GET/POST /api/config 接口会拦截未认证访问，且错误响应里不会泄露敏感配置。这堵住了配置面板这条潜在的信息泄漏通道。

**7. US-1280 — CLI 新增 `spiral doctor` 环境体检与自动修复建议**
一条命令就能检查 SPIRAL 环境是否健康：依赖是否齐全、prd.json 是否合法、是否有孤儿 worktree 等，并直接给出可粘贴的修复命令。健康返回 0、有问题返回 1，方便接入 CI 或开机自检。

**8. US-1275 — [安全测试] SettingsTab：阻止未授权的配置读写**
配套 US-1282 的另一组安全测试，更聚焦于 SettingsTab 的 GET 和 POST /api/config 端点，确保未认证请求被拒绝、错误信息不泄密。两组测试合起来形成完整的鉴权矩阵。

**9. US-1274 — Phase G：从故事验收标准自动生成 API 文档**
新建 `lib/api_doc_generator.py`，把每个故事的 acceptance criteria 转成带 docstring 的 Python 桩模块，再渲染成 `.spiral/docs/api/` HTML。Phase G 在故事提交后自动调用，让 API 文档永远跟代码同步。

**10. US-1269 — [E2E 测试] 通过 --resume 标志实现 SPIRAL 崩溃恢复**
SPIRAL 一直会在每个阶段后写 checkpoint，但没人测过恢复路径。这条故事给 spiral.sh 加上 --resume 标志，在 lib/checkpoint.sh 里实现 `checkpoint_read_and_resume`，并用 bats 集成测试在 Phase I 中途杀掉 SPIRAL，验证它能干净地从断点继续，不重跑前面的阶段。

---

**本轮总结：** 这10个改进围绕三个主题：(1) **可靠性与崩溃恢复** — E2E 集成测试、--resume 断点恢复、worker 崩溃自愈，让 SPIRAL 长跑更稳；(2) **配置与诊断的开发者体验** — `spiral config` 与 `spiral doctor` 命令把环境管理从手工编辑变成 CLI 一行；(3) **安全与文档自动化** — Settings 面板的鉴权双保险、Phase G 自动从 AC 生成 API 文档，让运维信任度和文档新鲜度同步提升。

**累计进度：** 52/66 (78.8%) — 本次运行已完成 52 个故事

---

# SPIRAL 最近10个改进 — 2026-04-27 (第60个故事里程碑)

> 自动生成：SPIRAL 每完成10个故事后更新此文件。

---

**1. US-1212 — Federated PRD：新增 prd.include 字段**
让 prd.json 通过 include 数组引入子项目的故事文件，Phase M 合并时自动加命名空间前缀（如 web:US-100）并防循环引用。这把多仓库管理统一到一个 SPIRAL 调度循环。

**2. US-1210 — 端到端集成测试：用模拟 Claude CLI 跑完整 SPIRAL 循环**
新增 `tests/test_spiral_e2e_integration.py`，monkeypatch 把 Claude CLI 调用全部假返回，跑完整 A→R→T→S→E→M→X→G→I→V→C 链。零 API 成本验证阶段顺序与最终状态。

**3. US-1219 — CLI 新增 `spiral config list` / `spiral config set`**
不必再编辑 spiral.config.sh，命令行就能列出或修改配置，原地写入文件并下次运行生效。

**4. US-1217 — spiral-ui 新增 useStoryTimeline Hook 与 Activity Log 标签**
为活动日志页打基础：解析 results.tsv 成时间线数组、注册路由，先呈现空状态占位符等待表格组件接入。

**5. US-1211 — 端到端测试：故事重试模型升级与 Worker 崩溃恢复**
两个测试：模拟失败验证 haiku→sonnet 升级写入 results.tsv；模拟 worker 崩溃验证主循环重启 worker 并完成剩余故事。

**6. US-1292 — Phase R：研究结果 24 小时缓存**
把 Gemini 网络搜索结果缓存到 `.spiral/research-cache/`，TTL 24 小时，缓存键用 hash(query_string)。重复运行时省 API、加速 Phase R。

**7. US-1287 — 集成测试：Phase S 拒绝 antiPattern 故事并警告空 AC**
在 Phase S 边界用例套件里增加测试：fixture PRD 含一条匹配 antiPattern 规则的故事和一条 acceptanceCriteria 为空的故事，验证拒绝和警告路径。

**8. US-1284 — 新增 MockClaudeAPI fixture，让阶段行为测试可隔离**
当 `CLAUDE_CLI_MOCK=true` 时拦截 claude CLI 调用并返回预设响应（成功 / 超时 / 错乱 JSON 三种场景），让 SPIRAL 各阶段都能脱离真实 API 测试。

**9. US-1282 — [安全测试] spiral-ui Settings 标签的鉴权与权限保护**
SettingsTab 组件及其 GET/POST /api/config 接口必须拦截未认证访问，且错误响应不能泄露敏感数据。

**10. US-1280 — CLI 新增 `spiral doctor` 环境体检与自动修复建议**
一条命令检查依赖、prd.json、孤儿 worktree 等，输出可粘贴的修复命令，健康返回 0、有问题返回 1，方便接入 CI 与开机自检。

---

**本轮总结：** 这一轮的核心是 **测试基础设施与可观测性的全面升级**：(1) **端到端可测性** — MockClaudeAPI fixture (US-1284) + 完整 SPIRAL 循环 e2e 测试 (US-1210/1211) + Phase S 边界用例 (US-1287)，让 SPIRAL 自身在零 API 成本下可以被严格验证；(2) **运维与诊断** — `spiral doctor` (US-1280) 与 `spiral config` (US-1219) 把环境管理从手工编辑变成 CLI 一行；(3) **性能与可视化** — Phase R 缓存 (US-1292)、Activity Log 标签骨架 (US-1217)、Federated PRD 子项目组合 (US-1212)，分别让重复运行更快、运行历史更清晰、跨仓库协作成为可能。

**累计进度：** 60/78 (76.9%) — 本次运行已完成 60 个故事

---

# SPIRAL 最近10个改进 — 2026-04-26 (第70个故事里程碑)

> 自动生成：SPIRAL 每完成10个故事后更新此文件。

---

**1. US-1212 — 联合 PRD：通过 prd.include 字段组合子项目故事**
让 prd.json 可以引用多个子项目的故事文件，支持以命名空间前缀（如 web:US-100）合并并防止循环引用。这让大型多模块项目能够按子项目独立维护故事，再统一运行 SPIRAL。

**2. US-1210 — 端到端集成测试：完整 SPIRAL 循环（模拟 Claude CLI）**
新增 test_spiral_e2e_integration.py，用 monkeypatch 拦截 subprocess.run，模拟 Claude CLI 响应跑完整个阶段链。验证阶段执行顺序与最终 prd.json 状态，无需真实 API 即可保证主循环不退化。

**3. US-1219 — CLI 配置管理：spiral config list / set**
不需要再编辑 spiral.config.sh，直接用命令查看和修改运行时配置。`config list` 列出所有变量与说明，`config set KEY VALUE` 原地写回，下次运行立即生效。降低了配置门槛。

**4. US-1217 — spiral-ui 活动日志 Tab 数据层**
在仪表盘新增 Activity Log 标签，解析 results.tsv 为类型化的故事时间线数组。先注册路由和占位状态，为后续表格组件铺路。让用户能直接在 UI 上看到每个故事的处理流水。

**5. US-1211 — 端到端测试：故事重试升级与工人崩溃恢复**
两个新测试用例：一个验证故事失败后模型从 haiku 自动升级到 sonnet 并写入 results.tsv，另一个模拟 worker 崩溃后主循环能重启并继续完成剩余故事。守住自动恢复能力。

**6. US-1283 — Phase S 拒绝原因追踪与 explain-rejection 命令**
当故事在 Phase S 因违反宪法或重复被拒绝时，记录结构化原因。新增 `spiral explain-rejection <id>` 命令查询某条故事被拒理由。在大型 PRD 中调试为什么某个故事没合入变得透明可查。

**7. US-1301 — Phase X 依赖推理器（NLP + DAG 环检测）**
新增 lib/dependency_resolver.py：扫描故事标题和描述里的依赖关键词，自动构建依赖图、检测循环、输出 .spiral/dependencies.json 含 blocker 数和关键性评分。让实现顺序更聪明，避免被未完成依赖卡住。

**8. US-1292 — Phase R 研究结果缓存（24 小时 TTL）**
Gemini 网络搜索结果按查询哈希缓存到 .spiral/research-cache/，24 小时内同样的查询不再重复调 API。重复运行同一项目时大幅加速 Phase R，并降低成本。

**9. US-1287 — Phase S 边界测试：antiPattern 拒绝与空 AC 警告**
为 Phase S 边界场景套件添加两个测试：一个故事命中 antiPattern 规则应被拒绝，另一个 acceptanceCriteria 为空应触发警告。守住故事质量门槛不被退化代码绕过。

**10. US-1284 — MockClaudeAPI 测试夹具**
当设置 CLAUDE_CLI_MOCK=true 时拦截所有 claude CLI 调用，按预设返回响应。支持成功、超时、畸形 JSON 三种场景，让 phase 测试可以隔离运行不烧钱也不被网络抖动影响。

---

**本轮总结：** 这10个改进围绕三个主题：**(1) 测试覆盖与可重复性** — 端到端集成测试、模拟夹具、边界用例补齐了 SPIRAL 自身的测试金字塔；**(2) 调试与可观测性** — 拒绝原因追踪、配置 CLI、活动日志 UI 让黑盒变透明；**(3) 性能与扩展性** — 联合 PRD、研究缓存、依赖图把 SPIRAL 从单仓库小项目工具推向更大规模的工程实践。

**累计进度：** 70/79 (88.6%) — 本次运行已完成 70 个故事，剩 9 个待办

---

# SPIRAL 最近10个改进 — 2026-04-26 (第80个故事里程碑)

> 自动生成：SPIRAL 每完成10个故事后更新此文件。

---

**1. US-1289 — CLI 快照保存与恢复**
新增 `spiral snapshot save` 和 `spiral snapshot restore` 命令。前者把 .spiral/ 整个状态打包成 tar.gz，后者按时间戳恢复。让用户可以在重大变更前留快照、出错时回滚到已知良好状态。

**2. US-1288 — 时间序列指标导出**
新增 `spiral metrics export-timeseries` 命令，把 results.tsv 和 spiral_events.jsonl 解析成时间序列 JSON：每次迭代的累计 token、通过故事数、平均模型层级。支持把数据接入外部仪表盘做趋势分析，超越 spiral-ui 的可视化范围。

**3. US-1286 — 跨子项目联合搜索**
新增 `spiral search <query>` 命令，在主 prd.json 与所有 prd.include 引用的子项目中做全文搜索（标题、描述、验收标准）。结果按项目名、故事 ID、匹配分数返回。让大型联合项目的故事发现变得可行。

**4. US-1309 — spiral-ui 测试 Tab（前端）**
新增 TestsTab React 组件，通过 useTestResults hook 获取 /api/tests/summary，渲染故事 ID、测试状态、测试数、文件触碰验证、覆盖率的网格。点击行弹出失败日志详情模态框。把测试可见性带入仪表盘。

**5. US-1308 — spiral-ui 测试摘要后端**
新增 GET /api/tests/summary 路由，读取 spiral_events.jsonl，按 story_id 聚合验证事件，返回测试状态、计数、文件触碰结果、覆盖率。为 Tests tab 前端提供数据源。

**6. US-1306 — history_search 数据泄露安全测试**
为 US-1259 引入的 .spiral/history.db 与 find_similar_attempts() 编写安全测试，验证查询结果或错误消息不暴露 API key、token、密码等敏感执行数据。守住历史库的最小披露原则。

**7. US-1305 — history_search API 鉴权测试**
为 US-1259 的 history search 编写 pytest 安全测试，验证任何暴露 find_similar_attempts() 的 HTTP 端点或 CLI 包装拒绝未鉴权调用，并保证错误消息不泄露原始 SQLite 行数据。

**8. US-1304 — Config API 鉴权拦截测试**
为 US-1214 引入的 POST /api/config 端点写 pytest 安全测试。验证缺失鉴权头的请求返回 401/403，且错误响应永不泄露 token 或文件系统路径。把 spiral-ui 配置写入接口的安全门槛固化。

**9. US-1285 — trace-dependencies 回归测试**
为 US-1209 的 `spiral trace-dependencies` 命令写回归测试。用一个固定依赖链的 PRD 夹具调用命令，断言输出结构与循环依赖检测正确。防止依赖追踪能力在后续迭代中被悄悄破坏。

**10. US-1265 — 插件清单丰富化与 skills 子命令**
扩展 plugin.toml schema，新增 name、version、description、inputs、outputs、tags 字段（agentskills.io 风格元数据）。新增 `spiral skills list`（列举本地插件）和 `spiral skills install <url>`（拉取+校验+解压）。让 SPIRAL 插件生态有了规范的发布与安装流程。

---

**本轮总结：** 这10个故事围绕三个主题：**(1) 运维与状态管理** — 快照保存恢复、时间序列指标导出、联合搜索给运维方提供了"可回滚 + 可观测 + 可发现"的三件套；**(2) 测试可见性** — Tests tab 后端与前端把测试结果从命令行带入仪表盘，让非工程角色也能看到质量状况；**(3) 安全门槛** — 三条安全测试（Config API、history_search 鉴权、history_search 数据泄露）把已有功能的攻击面用回归测试封死。

**累计进度：** 80/89 (89.9%) — 本次运行已完成 80 个故事，剩 9 个待办

---
