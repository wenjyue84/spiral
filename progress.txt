## Iteration [N] - Story: Add analytics CSV export button to AnalyticsTab (US-1277)

### What was implemented
- Added `downloadAnalyticsCSV()` helper function that converts agentTelemetry array to CSV format
- CSV includes 8 columns: ts, workerId, storyId, fromPhase, toPhase, durationMs, qualityScore, retryCount
- Added Export CSV button (📥 Export CSV) in the status strip, positioned on the right with ml-auto
- Button uses client-side Blob and URL.createObjectURL for download (no server endpoint)
- Filename format: analytics-YYYY-MM-DD.csv (using current date via new Date().toISOString())
- Button disabled state when agentTelemetry is empty, showing tooltip "No telemetry data to export"
- Button styling: emerald-100/emerald-700 when enabled, slate-100/slate-400 when disabled

### Patterns discovered
- Export buttons in AnalyticsTab follow emerald/success color scheme
- CSV generation uses simple string concatenation with .join(',') and .join('\n')
- Button state management uses conditional className based on data availability (length === 0)
- Button positioning uses ml-auto in flex container to align right without affecting other controls

### Gotchas
- None - the implementation was straightforward with proper condition checking for empty data

### Files touched
- spiral-ui/src/components/AnalyticsTab.tsx (modified, +55/-1 lines)

### Quality gates passed
- ✓ ESLint validation: no unused variables or type errors
- ✓ All acceptance criteria met:
  - [x] Export CSV button appears in status strip of AnalyticsTab
  - [x] Downloads CSV with correct columns from agentTelemetry
  - [x] Filename uses format analytics-YYYY-MM-DD.csv
  - [x] Button is disabled/hidden when agentTelemetry is empty
  - [x] No server endpoint needed - purely client-side

## Iteration [N] - Story: Add rolling 3-iteration average overlay to velocity chart (US-1276)

### What was implemented
- Added rolling 3-iteration average calculation and visualization to the Iteration Velocity chart
- Calculated rolling average using window slicing: for each index i, average of kept values at [max(0, i-2), i]
- Overlaid SVG polyline on top of existing bar chart with amber/orange color (#f97316)
- Used SVG viewBox coordinate mapping to align polyline with bar chart positions
- Edge cases handled: when data length < 3, window automatically adjusts to available items

### Patterns discovered
- Chart overlays use SVG with position:absolute and matching viewBox dimensions
- Coordinate scaling: x as percentages (flex distribution), y as inverted percentages converted to pixels
- SVG polyline with proper stroke styling gives clean trend line visualization
- Using `vectorEffect="non-scaling-stroke"` prevents line thickness issues with viewBox scaling

### Gotchas
- None - the implementation was straightforward with proper edge case handling

### Files touched
- spiral-ui/src/components/AnalyticsTab.tsx (modified, +54/-11 lines)

### Quality gates passed
- ✓ ESLint validation: no unused variables or type errors
- ✓ All acceptance criteria met:
  - [x] Rolling 3-iter average line visible in amber/orange color
  - [x] Works correctly when iterationVelocity.length < 3
  - [x] SVG polyline uses same coordinate space as bars
  - [x] No API changes required

## Iteration [N] - Story: Add story complexity breakdown chart to AnalyticsTab (US-1275)

### What was implemented
- Fixed missing `estimatedComplexity` field in the storiesList API response in `analyticsApi.ts`
- The AnalyticsTab component already had the full complexity breakdown chart implementation:
  - CollapsibleSection titled "Complexity Breakdown" that displays when storiesList.length > 0
  - Bar chart showing small/medium/large complexity groups with pass rates
  - Proper colors: emerald for small, violet for medium, amber for large
  - Uses the same inline div bar pattern as iterationVelocity chart

### Patterns discovered
- The component implementation (AnalyticsTab.tsx) was already complete; the issue was in the data layer (API response)
- The prd.json stories already contain estimatedComplexity field, so no new data source was needed
- The pattern for inline bar charts in analytics is consistent: calculate max value, map items to flex containers with height styling

### Gotchas
- The story's implementation was split across two files: API response builder (analyticsApi.ts) and component (AnalyticsTab.tsx)
- The component defaulted to 'small' complexity when the field was missing, so the chart still rendered but with incorrect grouping

### Files touched
- spiral-ui/src/server/analyticsApi.ts (modified, +1 line)

### Quality gates passed
- ✓ TypeScript compilation (--noEmit)
- ✓ Code review: field correctly defaults to empty string when missing
- ✓ Follows existing patterns in storiesList API response

## Pre-analysis for US-1249 (Gemini 2.5 Pro)
**Correction:** This story is for a frontend E2E test, not a Frappe/Python backend task. The implementation will be in TypeScript/Python using Playwright or a similar E2E testing framework, targeting the existing React UI.

**Technical Brief for US-1249**

**Objective:** Create a Playwright E2E test to validate the filter controls on the analytics retry history dashboard.

**Target Components (for testing):**
*   `spiral-ui/src/components/analytics/FailureRetryDashboard.tsx`
*   `spiral-ui/src/components/AnalyticsTab.tsx`

**Test File to Create:**
*   A new test file, likely in `tests/e2e/` or `tests/playwright/`, named e.g., `test_retry_history_filters.py`.

**Implementation Approach:**
1.  Navigate to the analytics page containing the retry history table.
2.  Locate the three new filter dropdowns: "Failure Type," "Model Tier," and "Outcome."
3.  Write tests to select an option in each dropdown and assert the table content updates correctly.
4.  Test combinations of filters to ensure they work together.
5.  Verify the "empty state" message appears when filters result in no matching rows.
6.  Confirm the existing "Most Recent" / "Most Retried" sort toggle continues to function alongside the filters.

**Gotchas & Edge Cases:**
*   **Asynchronicity:** Table updates are asynchronous. Use explicit waits (e.g., `expect(locator).to_be_visible()`) after applying filters to prevent flaky tests.
*   **Test Data:** The test environment may need seed data to guarantee all filter conditions can be reliably triggered and asserted.
*   **Selectors:** Use stable, test-specific selectors (like `data-testid`) for filters and the results table to avoid brittleness.
## Iteration 1 - Sun, Apr 12, 2026  6:11:52 PM
Completed: [E2E Test] Add filter controls to analytics retry history table (ID: US-1249) in 8m


## Iteration [N] - Story: Phase G: Generate CHANGELOG.md from Story Commits Using git-cliff (US-1253)

### What was implemented
- Created `lib/phases/phase_g.sh` - thin orchestration stub that sources `gen_changelog.sh` and invokes `phase_gen_changelog()` for changelog generation
- Enhanced `cliff.toml` with improved documentation and comments explaining story ID grouping support
- Created `tests/test_phase_g_changelog.bats` - comprehensive BATS test suite with 18 tests validating:
  - git-cliff binary availability and version checking
  - cliff.toml config parsing and validation
  - CHANGELOG.md generation with proper structure
  - Commit grouping by type (Features, Bug Fixes, etc.)
  - Markdown formatting and link generation
  - Orphan commit detection (commits without story IDs)
  - Test isolation with temporary git repos

### Patterns discovered
- The project already had significant changelog infrastructure in place:
  - `lib/phases/gen_changelog.sh` with the core `phase_gen_changelog()` function
  - `lib/gen_changelog.py` with Python implementation supporting both generation and append modes
  - `cliff.toml` with git-cliff configuration
- Phase G was being used via CLI subcommands but needed a proper phase orchestration stub for the main phase loop
- BATS tests for bash/shell functionality follow consistent patterns with setup/teardown, helper functions, and assertion libraries
- The story ID pattern (US-NNN, UT-NNN, FE-NNN, BE-NNN) is consistently used in commit bodies with "Story: <ID>" footer format

### Gotchas
- BATS files use special syntax (@test decorator) that can't be validated with plain `bash -n`
- git-cliff tests are slow because they require creating temporary git repos and running git-cliff binary
- The story description mentioned ".cliff.toml" (with dot prefix), but the codebase uses "cliff.toml" (without dot); clarified via code inspection
- git-cliff's standard output grouping is by commit type, not by story ID - full story ID-based grouping would require post-processing

### Files touched
- lib/phases/phase_g.sh (new, 45 lines)
- tests/test_phase_g_changelog.bats (new, 232 lines)
- cliff.toml (modified, +5 lines documentation)

### Quality gates passed
- ✓ shellcheck validation on phase_g.sh
- ✓ Syntax validation on test file
- ✓ File sourcing tests (both phase_g.sh and gen_changelog.sh source without errors)
- ✓ git add and git commit successful
- ✓ BATS individual test runs successful (spot-checked multiple tests)


## Iteration [N] - Story: Dashboard API: GET /api/dashboard/throughput (US-1254)

### What was implemented
- Created `lib/dashboard/throughput.py` with `aggregate()` function that:
  - Parses results.tsv using existing parse_results_tsv() from lib.results_tsv
  - Filters records to status='passed' only
  - Extracts current iteration from .spiral/_checkpoint.json
  - Groups records by completion hour (ISO format: YYYY-MM-DDTHH:00)
  - Implements 5-second cache with global _THROUGHPUT_CACHE and _CACHE_TIMESTAMP
  - Returns sorted list of hourly aggregations with count and story_id list

- Updated `lib/dashboard/api.py` to add GET /api/dashboard/throughput endpoint:
  - Added import for aggregate_throughput
  - Added endpoint route that calls aggregate()
  - Returns JSON with hourly_throughput list and total_hours count
  - Includes error handling with logging

- Created `tests/test_dashboard_throughput_api.py` with 8 comprehensive tests:
  - test_hourly_aggregation: validates basic grouping by hour
  - test_filters_by_iteration: ensures filtering to current iteration only
  - test_filters_by_status: verifies only 'passed' status included
  - test_caching_behavior: validates 5-second cache TTL expiry
  - test_malformed_timestamps_ignored: confirms graceful handling of invalid timestamps
  - test_missing_files_returns_empty: handles missing results.tsv
  - test_no_checkpoint_filters_all_iterations: includes all iterations when no checkpoint
  - test_sorted_by_hour: verifies results sorted ascending by hour

### Patterns discovered
- The project uses global module-level caching with time.time() for TTL management
- FastAPI endpoints in lib/dashboard/api.py follow a consistent pattern: load data, filter, aggregate, return JSON with error handling
- Tests use temporary directories via pytest fixtures and the write_results_tsv() utility for creating test data
- Cache must be cleared per-test using autouse fixture to prevent test pollution

### Gotchas
- Initial hour formatting bug: used `.strftime("%Y-%m-%dT%H:%M").replace(":%M", ":00")` which was broken; fixed to `.strftime("%Y-%m-%dT%H:00")`
- Global cache persisted across tests; solved with autouse fixture that clears _THROUGHPUT_CACHE and _CACHE_TIMESTAMP before each test
- Ruff import sorting required all stdlib imports (json, time, datetime, pathlib, typing) before relative imports
- Mypy strict mode required explicit type guard on cached value: `if isinstance(cached, list): return cached`

### Files touched
- lib/dashboard/throughput.py (new, 100 lines)
- lib/dashboard/api.py (modified, +30 lines)
- tests/test_dashboard_throughput_api.py (new, 422 lines)
- prd.json (modified, marked US-1254 passes: true)

### Quality gates passed
- ✓ All 8 pytest tests pass
- ✓ mypy --strict on both new files (lib/dashboard/throughput.py, lib/dashboard/api.py)
- ✓ ruff check passed (import sorting, F401 unused imports)
- ✓ Git commit successful with full acceptance criteria tracking

## Iteration 1 - Sun, Apr 12, 2026  6:49:35 PM
Completed: Dashboard API: GET /api/dashboard/throughput (Stories Completed Per Hour) (ID: US-1254) in 7m
## Iteration 1 - Sun, Apr 12, 2026  7:13:16 PM
Completed: [Multi-Repo] Validate Federated Story IDs Match Sub-Project Namespace Prefixes (ID: US-1255) in 6m

## Iteration 2 - Sun, Apr 12, 2026  7:20:26 PM
Completed: Fix Vulture Dead Code: Remove Unused Variables in overflow_guard and conflict_resolver (ID: US-1267) in 6m
## Iteration 1 - Sun, Apr 12, 2026  7:34:06 PM
Completed: Fix SBOM Generation: Update cyclonedx-py CLI Flag from --outfile (ID: US-1271) in 2m

## Iteration [N] - Story: Fix Bats/SARIF CI: Remove Broken Submodule and Update CodeQL Action SHA (US-1272)

### What was implemented
- Removed `.claude/worktrees/agent-ada52e4b` from git tracking (was incorrectly treated as submodule)
  - Added `.claude/worktrees/` to `.gitignore`
  - Ran `git rm --cached` to remove the tracked directory
- Updated CodeQL action to valid pinned SHA for v3.26.6
  - Changed from: `4fa2a7953630fd2f3aab3b2c1b7c6eb76a5a5beb` (invalid/non-existent)
  - Changed to: `4dd16135b69a43b6c8efb853346f8437d92d3c93` (verified via GitHub API)
  - Updated both shellcheck and semgrep SARIF upload steps in `.github/workflows/ci.yml`

### Patterns discovered
- The stale `.claude/worktrees/agent-ada52e4b` was a leftover from a previous worker run
- GitHub CodeQL action SHA validation requires checking against the repository's release tags
- Use GitHub API endpoint `https://api.github.com/repos/github/codeql-action/git/refs/tags/vX.Y.Z` to resolve tag commit SHAs

### Gotchas
- The initial invalid SHA `4fa2a7953630fd2f3aab3b2c1b7c6eb76a5a5beb` was likely a typo or corrupted reference
- Working directories created by git worktrees should always be in .gitignore to prevent CI failures

### Files touched
- .gitignore (modified, +1 line)
- .github/workflows/ci.yml (modified, 2 lines changed)
- Deleted: .claude/worktrees/agent-ada52e4b (removed from git tracking)

### Quality gates passed
- ✓ Pre-commit checks passed for modified files
- ✓ YAML syntax validation (structure is valid)
- ✓ Shell linting passed
- ✓ Git commit successful
- ✓ prd.json updated with passes: true

## Iteration 2 - Sun, Apr 12, 2026  7:40:05 PM
Completed: Fix Bats/SARIF CI: Remove Broken Submodule and Update CodeQL Action SHA (ID: US-1272) in 5m


## Pre-analysis for US-1274 (Gemini 2.5 Pro)
**Technical Brief for Claude Code Agent**

This user story is for a React/TypeScript frontend task, not a Frappe/Python one. The goal is to create a new donut chart component to visualize model cost breakdown.

**1. Files to Modify:**
- **Create:** `spiral-ui/src/components/analytics/ModelCostDonut.tsx`
- **Modify:** `spiral-ui/src/components/AnalyticsTab.tsx` (to import and use the new component)
- **Modify:** `spiral-ui/src/components/analytics/index.ts` (to export the new component)

**2. Frappe DocTypes/Hooks:**
- None. This is a pure frontend component implementation in the `spiral-ui` React application.

**3. Recommended Implementation Approach:**
- In `ModelCostDonut.tsx`, create a component that accepts a `modelPerformance` array prop.
- Calculate total cost from the array, then derive the percentage for each model.
- Use CSS `conic-gradient` to build the chart, mapping model names to specified colors: haiku (blue), sonnet (violet), opus (amber).
- Position percentage labels over the corresponding chart segments.
- In `AnalyticsTab.tsx`, import `ModelCostDonut` and render it inside a `CollapsibleSection`, conditioned on `modelPerformance.length > 0`.

**4. Edge Cases & Gotchas:**
- Handle the case where total cost is zero to prevent division-by-zero errors.
- Ensure the `modelPerformance` data structure is correctly unpacked to get model names and cost values.
- Implement a default color for any models not explicitly named (haiku, sonnet, opus).
- The "donut" hole can be created using a radial gradient mask or an overlaid pseudo-element.

## Iteration 1 - Tue, Apr 14, 2026  2:25:22 AM
Completed: Add model cost-split donut chart to AnalyticsTab (ID: US-1274) in 5m

## Iteration 2 - Tue, Apr 14, 2026  2:32:31 AM
Completed: Add iteration range filter pills to AnalyticsTab (ID: US-1281) in 6m

## Iteration 3 - Tue, Apr 14, 2026  2:36:37 AM
Completed: Add attempt timeline swimlane to StoryDetailPanel (ID: US-1282) in 3m

## Iteration 1 - Tue, Apr 14, 2026  2:54:27 AM
Completed: Add story complexity breakdown chart to AnalyticsTab (ID: US-1275) in 5m

## Iteration 2 - Tue, Apr 14, 2026  3:01:19 AM
Completed: Add rolling 3-iteration average overlay to velocity chart (ID: US-1276) in 6m

## Iteration 3 - Tue, Apr 14, 2026  3:05:47 AM
Completed: Add analytics CSV export button to AnalyticsTab (ID: US-1277) in 3m

## Iteration 1 - Tue, Apr 14, 2026  3:19:48 AM
FAILED diagnosis-block gate: Add SLA breach badges to PhaseTimingBars (ID: US-1278) — no diagnosis block found — attempt 1/3


## Iteration N - Story: Add SLA breach badges to PhaseTimingBars (US-1278)

### What was implemented
- SLA_THRESHOLDS constant added to PhaseTimingBars.tsx (A=30s, R=300s, S=60s, M=30s, I=600s, V=300s, C=30s)
- getSLABadgeType() helper returns 'red' at >=2x SLA, 'amber' at >=1.5x SLA, null otherwise
- Badge rendered inline inside phase label span using emoji + "SLA" text with color classes

### Patterns discovered
- Previous attempt had all code correct but failed the diagnosis block gate — always output diagnosis block BEFORE any file edits
- PhaseTimingBars uses t.phase.charAt(0) to extract single-letter phase key for SLA lookup

### Gotchas
- Diagnosis block must be plain text output BEFORE any Edit/Write/Bash file-modifying calls
- Story was already implemented from attempt 1; attempt 2 only needed verification + prd.json update
## Iteration 2 - Tue, Apr 14, 2026  3:24:54 AM
Completed: Add SLA breach badges to PhaseTimingBars (ID: US-1278) in 1m


## Iteration [N] - Story: Add top-5 costliest stories table to AnalyticsTab (US-1279)

### What was implemented
- Added new "Costliest Stories" CollapsibleSection to AnalyticsTab
- Table displays the top 5 longest-running stories from bottlenecks.longest array
- Four columns in the table:
  * Rank (1-5, centered)
  * Story ID (clickable button that opens StoryDetailPanel)
  * Title (truncated with tooltip)
  * Duration in minutes (right-aligned, bold)
- CollapsibleSection conditionally renders when bottlenecks.longest.length > 0
- Uses amber badge color (bg-amber-100 text-amber-700) consistent with alert/warning styling
- Story ID button styled as blue clickable link with hover underline effect

### Patterns discovered
- Reused existing table styling pattern from resolvedFailures section
- CollapsibleSection components accept title, badge (count), and badgeColor props
- Button elements inside table cells use onClick handlers to update parent state
- Table rows use hover:bg-slate-50 for visual feedback
- Rank calculation uses array map index (i + 1) for 1-based numbering

### Gotchas
- None - the implementation was straightforward using existing data structures and patterns

### Files touched
- spiral-ui/src/components/AnalyticsTab.tsx (modified, +33 lines)
- prd.json (marked US-1279 passes: true)

### Quality gates passed
- ✓ Code follows existing table styling patterns
- ✓ No new API endpoints required
- ✓ Uses existing bottlenecks.longest data structure
- ✓ Story ID click properly integrates with existing setSelectedStoryId state
- ✓ All acceptance criteria met:
  - [x] CollapsibleSection Costliest Stories appears when bottlenecks.longest.length > 0
  - [x] Table shows rank, clickable story ID, title, durationMin
  - [x] Clicking story ID sets selectedStoryId to open StoryDetailPanel
  - [x] Limited to top 5 entries using .slice(0,5)
  - [x] Uses existing bottlenecks.longest data with no API changes
## Iteration 3 - Tue, Apr 14, 2026  3:29:17 AM
Completed: Add top-5 costliest stories table to AnalyticsTab (ID: US-1279) in 3m


## Iteration [N] - Story: Add keyword search filter to StoriesListAccordion (US-1280)

### What was implemented
- Restructured StoriesListAccordion layout to separate search input from filter buttons
- Moved search input to its own row appearing above the filter tabs
- Updated placeholder text from "Search story ID or title…" to "Search stories…"
- Changed input width from fixed w-56 to full-width (w-full)
- Removed ml-auto positioning class since input is no longer inline with buttons

### Patterns discovered
- Analytics components follow a vertical layout pattern: search → filter pills → results
- Input placeholder text follows abbreviated format with ellipsis (e.g., "Search stories…")
- Full-width search inputs use w-full with mb-3 for spacing above filter buttons

### Gotchas
- None - layout restructuring was straightforward with no logic changes needed

### Files touched
- spiral-ui/src/components/analytics/StoriesListAccordion.tsx (modified, +10/-8 lines)

### Quality gates passed
- ✓ Component structure intact: no props changes, filtering logic preserved
- ✓ All acceptance criteria met:
  - [x] Search input appears above filter tabs
  - [x] Case-insensitive filtering on id and title
  - [x] Search combines with status filter via AND logic
  - [x] Clearing input restores unfiltered list
  - [x] No changes to component interface
- ✓ Git commit successful with full acceptance criteria tracking
## Iteration 4 - Tue, Apr 14, 2026  3:33:12 AM
Completed: Add keyword search filter to StoriesListAccordion (ID: US-1280) in 3m
## Iteration 1 - Fri, Apr 17, 2026 10:38:08 PM
FAILED diff-guard: [Security Test] spiral-ui: SettingsTab access control (US-1215) (ID: US-1256) — 3002 lines > 800 limit — attempt 1/3


## Iteration 2 - Story: [Security Test] spiral-ui: SettingsTab access control (US-1256)

### What was implemented
- Created tests/test_security_us1215.py with 4 security tests (~45 lines)
- Tests use FastAPI TestClient against lib/dashboard/api.py with SPIRAL_DASHBOARD_API_KEY auth
- Verifies: unauthenticated → 401, wrong key → 403, valid key → allowed, no secrets leaked

### Patterns discovered
- Dashboard API auth is middleware-based (_APIKeyMiddleware) — all endpoints protected uniformly
- Previous attempt produced 3002-line diff; minimal 45-line test file + prd.json change = ~51 lines

### Gotchas
- Acceptance criteria reference module-level test functions, not class methods — use bare functions not TestClass
- No /api/config endpoint exists; SettingsTab uses props + /api/save-config Vite middleware
- Pre-existing mypy errors in 3 other files (reachability_verifier, ucb1_select, spiral_dashboard) — unrelated
## Iteration 2 - Fri, Apr 17, 2026 10:48:19 PM
Completed: [Security Test] spiral-ui: SettingsTab access control (US-1215) (ID: US-1256) in 9m


## Pre-analysis for US-1257 (Gemini 2.5 Pro)
### Technical Brief: US-1257 - Inject Lessons into Decomposition Prompt

**Objective:** Enhance the story decomposition process by injecting relevant "learned lessons" into the prompt, creating a feedback loop from past issues to future planning.

**1. Python Files to Modify:**
*   `lib/decompose_story.py`: This is the primary file. It will be modified to import and use the lesson querying functionality.
*   `tests/test_decompose_lessons.py`: A new unit test file to be created to verify the injection logic.

**2. Frappe DocTypes / Hooks:**
*   None. This user story pertains to the Spiral project's internal Python scripts, not a Frappe application. The logic is contained within standard Python modules.

**3. Implementation Approach:**
1.  In `lib/decompose_story.py`, import `query_lessons` from `lib.learned_lessons`.
2.  Locate the function responsible for building the decomposition prompt.
3.  Implement a check for the `SPIRAL_LESSON_INJECTION` environment variable. If it is set to `false`, the lesson injection logic should be skipped.
4.  Before generating the final prompt, call `query_lessons()`, passing keywords or tags from the current story being decomposed.
5.  If relevant lessons are returned, format the top 3 into a clear, designated section (e.g., "### Known pitfalls to avoid:") and append it to the prompt string.
6.  Create `tests/test_decompose_lessons.py` to test the logic, mocking the `query_lessons` return value and asserting that the final prompt correctly includes the lesson text.

**4. Edge Cases & Gotchas:**
*   **No Lessons Found:** The logic must gracefully handle cases where `query_lessons()` returns no results; the prompt should be generated normally without an empty "pitfalls" section.
*   **Prompt Formatting:** The injected lessons must be formatted clearly to avoid confusing the language model. A structured prefix is recommended.
*   **Performance:** `query_lessons` reads from a file (`.spiral/learned_lessons.jsonl`). Ensure this operation is efficient and doesn't significantly slow down the decomposition process.


## Iteration [N] - Story: Phase I decompose: inject learned-lessons into decomposition prompt (US-1257)

### What was implemented
- Added `build_lessons_section()` to `lib/workers/decompose_story.py` that calls `query_lessons()` and formats top-K matching lessons into an XML block
- Added `--lessons` CLI arg (default: `.spiral/learned_lessons.jsonl`)
- Lessons injected after the base prompt; gated by `SPIRAL_LESSON_INJECTION` env var (default: true)
- Added `sys.path.insert` for `lib/` so `from learned_lessons import query_lessons` resolves
- Created `tests/test_decompose_lessons.py` with 5 tests covering all acceptance criteria

### Patterns discovered
- `lib/workers/` uses `sys.path.insert(0, os.path.dirname(__file__))` for local imports; parent `lib/` imports need a second insert one level up
- Token overlap in `query_lessons()` is Jaccard similarity — works well when story title/description shares keywords with lesson patterns

### Gotchas
- `from __future__ import annotations` required for `dict[str, Any]` in function signatures under Python 3.13 strict mypy
- Remove unused `pytest` import or ruff F401 fails
## Iteration 1 - Fri, Apr 17, 2026 11:07:32 PM
Completed: Phase I decompose: inject learned-lessons into decomposition prompt (close the learning loop) (ID: US-1257) in 8m


## Pre-analysis for US-1258 (Gemini 2.5 Pro)
Based on the user story JSON, here is a technical brief for the implementing agent. The user's mention of Frappe/Python appears to be a misinterpretation; the story pertains to the `Ralph` shell-based tooling within the Spiral project.

**Subject: Technical Brief for US-1258: Ralph Learned Lessons Injection**

**1. Files to Modify / Create:**
*   `ralph/ralph.sh`: The main script to orchestrate lesson injection.
*   `ralph/CLAUDE.md`: The prompt template that needs to reference the lessons.
*   **New File:** A Python helper script, e.g., `lib/lesson_retriever.py`, to handle the logic of finding relevant lessons.
*   **New File:** A BATS test file, `tests/test_ralph_lessons.bats`, for integration testing.

**2. Involved Frappe DocTypes or Hooks:**
*   None. This task is confined to the command-line interface and does not interact with the Frappe framework.

**3. Recommended Implementation Approach:**
1.  **Modify `ralph/ralph.sh`:**
    *   Add a check for the `SPIRAL_LESSON_INJECTION` environment variable. If it is `false`, skip the following steps.
    *   Invoke the new Python script (`lib/lesson_retriever.py`) to fetch lessons, passing the story details. Pipe the output to a sidecar file: `.spiral/ralph-lessons.md`.
2.  **Create `lib/lesson_retriever.py`:**
    *   This script should read a source of "learned lessons" (e.g., a project-local `lessons.jsonl`).
    *   It will use the input story description to perform a similarity search (e.g., keyword-based) to find the top-K most relevant lessons.
    *   It then formats and prints the lessons to stdout as Markdown.
3.  **Update `ralph/CLAUDE.md`:**
    *   Add a section that conditionally includes the sidecar's content. Example: `if [[ -f ".spiral/ralph-lessons.md" ]]; then cat .spiral/ralph-lessons.md; fi`.
4.  **Create `tests/test_ralph_lessons.bats`:**
    *   Write tests to assert that the sidecar file is created and its contents are passed to the final command when the feature is enabled, and that they are absent when disabled.

**4. Edge Cases & Gotchas:**
*   The lesson data source is not defined in the story; a simple `lessons.jsonl` file is a reasonable assumption.
*   The system must handle cases where no relevant lessons are found; the Python script should output nothing or a "No lessons found" message, and the prompt should render gracefully.
*   The temporary sidecar file (`ralph-lessons.md`) should be cleaned up after the main script executes to prevent state pollution.



## Iteration 2 - Story: Ralph prompt: inject relevant learned-lessons sidecar into Claude CLI call at runtime (US-1258)

### What was implemented
- Created `lib/write_ralph_lessons.py` — Python helper that reads `.spiral/learned_lessons.jsonl`, calls `query_lessons()`, writes a per-story `.spiral/ralph-lessons-<story>.md` sidecar
- Added learned-lessons sidecar injection section to `ralph/lib/prompt_builder.sh` — generates sidecar before each Ralph call, injects content into `RALPH_USER_PROMPT`
- Gated by `SPIRAL_LESSON_INJECTION` env var (default: true, disabled when =false)
- Created `tests/test_ralph_lessons.bats` with 4 tests covering injection, sidecar file creation, disable flag, and missing file handling

### Patterns discovered
- `lib/learned_lessons.py` imports `lib.failure_categorizer` which requires project root on sys.path — need `sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))` in addition to dirname insert
- Prompt builder uses `${SPIRAL_PYTHON:-python3}` for Python invocation — tests must ensure python is resolvable

### Gotchas
- The `python3` alias may not be available on all platforms in bats; resolved by checking `command -v python3` with fallback to `python`
## Iteration 2 - Fri, Apr 17, 2026 11:18:08 PM
Completed: Ralph prompt: inject relevant learned-lessons sidecar into Claude CLI call at runtime (ID: US-1258) in 9m


## Pre-analysis for US-1259 (Gemini 2.5 Pro)
This technical brief is for a Python-based implementation, not a Frappe application as mentioned in the prompt. The user story's content is unrelated to the Frappe framework.

**Project:** Spiral
**Feature:** FTS5-based search for historical execution results.

**1. Files to Modify:**
*   `lib/history_search.py`: New file to house the core indexing and search logic.
*   `lib/results_tsv.py`: Modify the append/write function to trigger re-indexing.
*   `tests/test_history_search.py`: New property-based test file.
*   `.gitignore`: Add `.spiral/history.db` to prevent committing the index.

**2. Involved DocTypes/Hooks:**
*   None. This is not a Frappe application. The integration point is the file write operation in `lib/results_tsv.py`.

**3. Implementation Approach:**
*   **`history_search.py`**:
    *   Use the standard `sqlite3` library. Create a virtual table using FTS5.
    *   `build_index()`: Wipes and rebuilds `.spiral/history.db`. It should parse `results.tsv`, find failure snippets (likely in `.spiral/crashes/`), and read recent git commit subjects (`git log ...`). It will concatenate these into a single text field for indexing.
    *   `find_similar_attempts(query, k)`: Executes an FTS `MATCH` query against the index, retrieving the top `k` results based on relevance (e.g., BM25).
*   **`results_tsv.py`**:
    *   In the function that appends a row, add a call to a debounced re-indexing function. A simple file-based timestamp check can achieve the 1-second debounce requirement.

**4. Edge Cases & Gotchas:**
*   The exact paths for "failure snippets" need to be located or assumed (e.g., `.spiral/crashes/*.txt`).
*   The re-indexing process should run in a non-blocking way if `results_tsv.py` is used in a performance-sensitive context.
*   The property test must carefully construct synthetic data and queries to ensure the recall measurement is meaningful.
*   Ensure the host system's SQLite version supports FTS5 (standard in modern Python).


## Iteration 3 - Story: Historical FTS5 index over results.tsv + failure snippets (US-1259)

### What was implemented
- Created `lib/history_search.py` with `build_index()` and `find_similar_attempts(query, k)`
- SQLite FTS5 virtual table with `unicode61 remove_diacritics 2` tokenizer for case-insensitive search
- `build_index()` parses results.tsv + reads crash snippets from `.spiral/crashes/<story_id>.txt`
- `find_similar_attempts()` uses FTS5 MATCH + rank ordering (BM25), handles bad queries gracefully
- Added `append_results_row()` and `_debounced_reindex()` to `lib/results_tsv.py`
- Debounce: timestamp file `.spiral/history_reindex_ts` gates reindex to max 1/second
- Created `tests/test_history_search.py` with 9 tests including N=50 recall property test

### Patterns discovered
- `.spiral/` already gitignored — `history.db` is automatically excluded
- SQLite FTS5 is standard in Python 3.13 — no extra deps needed
- `importlib.util.find_spec` + `exec_module` pattern allows lazy optional module loading without circular imports

### Gotchas
- `time.monotonic()` resets between processes — debounce uses file mtime for cross-process safety
- FTS5 `MATCH` raises `sqlite3.OperationalError` for invalid queries (e.g. bare OR/AND) — must catch it
- `ruff format` expands inline dict/tuple comprehensions — budget extra lines

## Pre-analysis for US-1260 (Gemini 2.5 Pro)
This technical brief is for implementing a FastMCP server for SPIRAL, based on the user story JSON. The initial prompt mentioning Frappe/lhdn_payroll_integration is disregarded as it contradicts the story's content.

**1. Files to Modify:**
*   `lib/mcp/spiral_mcp.py`: New file for the main server logic.
*   `pyproject.toml`: To add the `fastmcp` dependency.
*   `README.md`: To add documentation for using the new MCP server.
*   `prd.json`: Will be read by multiple tools and modified by `spiral_add_story`.

**2. Key Components:**
*   **Server:** A `fastmcp` server running over stdio, defined in `lib/mcp/spiral_mcp.py`.
*   **Data Sources:**
    *   `prd.json`: The main source for user stories.
    *   `.spiral/_checkpoint.json`: For current iteration status.
    *   `results.tsv`: For recent failure data.
*   **Validation:** `lib/prd_schema.py` will be used to validate new stories.

**3. Implementation Approach:**
*   In `lib/mcp/spiral_mcp.py`, create a `FastMCP` instance.
*   Implement five tool functions:
    *   `spiral_status()`: Reads and returns content from `.spiral/_checkpoint.json`.
    *   `spiral_stories(filter)`: Reads `prd.json`, filters stories, and returns a JSON array.
    *   `spiral_recent_failures(n)`: Reads the last `n` failure records from `results.tsv`.
    *   `spiral_iteration(n)`: Retrieves specific iteration data (likely from `_checkpoint.json`).
    *   `spiral_add_story(body, source)`: Validates input against `lib/prd_schema.py`, then appends the new story to `prd.json`.

**4. Edge Cases & Gotchas:**
*   Ensure the `spiral_add_story` operation on `prd.json` is atomic to prevent data corruption.
*   Gracefully handle parsing errors for all data source files (JSON, TSV).
*   Return values from all tools must be strictly JSON-serializable.
*   The `spiral_add_story` tool must return a clear error if schema validation fails.


## Iteration 4 - Story: MCP server exposing SPIRAL state (US-1260)

### What was implemented
- Created `lib/mcp/__init__.py` — transparent proxy to real `mcp` SDK (solves sys.path shadowing)
- Created `lib/mcp/spiral_mcp.py` — FastMCP server with 5 tools over stdio
- Added `fastmcp>=2.0` to `lib/pyproject.toml`
- Added SPIRAL MCP Server section to README.md with .mcp.json setup instructions

### Patterns discovered
- The editable-install `.pth` file adds `lib/` to sys.path hundreds of times — `lib/mcp/` permanently shadows the real `mcp` SDK package
- Fix: `lib/mcp/__init__.py` uses `importlib.util.spec_from_file_location` to load the real SDK from `.venv/Lib/site-packages/mcp/` directly, then registers it in `sys.modules["mcp"]`
- Must preserve `__name__`, `__spec__`, `__path__`, `__file__` in globals() after proxy load — otherwise Python can't locate `lib.mcp.spiral_mcp` submodule
- prd_schema loaded via `spec_from_file_location` (not sys.path insert) to avoid further shadowing

### Gotchas
- `globals().update(vars(real_mcp))` overwrites `__path__` → breaks submodule lookup. Only export public attrs.
- `importlib.util.find_spec("mcp")` checks sys.modules first — can cause recursion if called from within __init__.py while mcp is being loaded. Use `spec_from_file_location` with explicit path instead.
## Iteration 1 - Fri, Apr 17, 2026 11:53:15 PM
Completed: MCP server exposing SPIRAL state (lib/mcp/spiral_mcp.py) for Claude Code integration (ID: US-1260) in 16m


## Iteration 2 - Story: CLI spiral query stories (US-1235)

### What was implemented
- Fixed `AttributeError` in `lib/story_query.py:69` — `tsv.get("model")` was called without None guard when story has no results.tsv entry
- All 3 files were already present from prior attempt: `lib/story_query.py`, `lib/cli_subcommands.sh` (query handler), `tests/test_story_query.py`
- One-line fix: `model = (tsv.get("model") if tsv else None) or story.get("model") or ""`

### Patterns discovered
- When prior attempt fails due to test errors, check the actual test output first — often the implementation is mostly complete, just needs a small bug fix
- ruff format auto-expands multi-line expressions; pre-commit reformatted story_query.py lines automatically

### Gotchas
- Pre-existing mypy errors in other files (reachability_verifier.py, routing/ucb1_select.py, ui/spiral_dashboard.py) cause the mypy pre-commit hook to fail — these are NOT caused by this story's changes and should be ignored for this story
## Iteration 2 - Fri, Apr 17, 2026 11:58:06 PM
Completed: CLI: Add `spiral query stories` with filtering, sorting, and table/JSON/CSV export formats (ID: US-1235) in 4m


## Iteration 3 - Story: CLI spiral explain <story-id> (US-1236)

### What was implemented
- Created `lib/story_formatter.py` with `format_story_explain(story, results_row, fmt)` supporting text/markdown/json
- Text format: box header with ID/title/source badge, then Description, AC, Technical Notes, Files, Research Context, Run Metadata
- Markdown format: # heading, badges, ## sections for each group — suitable for Notion/docs
- JSON format: raw story object merged with `_results` key from results.tsv
- Research context section: only shown for `_source == "research"` stories; URLs with credibility (high/medium/low based on domain)
- Added `--explain <story-id> [--markdown|--json]` flag parsing to spiral.sh
- Added EXPLAIN_MODE handler to lib/cli_subcommands.sh
- Created `tests/test_story_formatter.py` with 14 passing tests

### Patterns discovered
- Ruff E741 flags ambiguous single-letter variable names (l, O, I) in comprehensions — use descriptive names like `lw` instead of `l`
- Pre-existing mypy errors in reachability_verifier.py, routing/ucb1_select.py, ui/spiral_dashboard.py are NOT caused by this story — confirmed in progress.txt from prior iteration
- `python lib/story_formatter.py prd.json <story-id>` works standalone without going through bash (useful for testing)

### Gotchas
- mypy pre-commit hook fails due to pre-existing errors in other files; this is expected and documented
## Iteration 1 - Mon, Apr 27, 2026 12:47:08 AM
FAILED diff-guard: Phase V: Integrate evidence aggregator into spiral.sh and update prd.schema.json (ID: US-1246) — 3209 lines > 800 limit — attempt 1/3


## Iteration N - Story: Trajectory export results.tsv + worker logs to JSONL (US-1264)

### What was implemented
- Created lib/export_trajectories.py: reads results.tsv, maps keep->1.0/reject->-1.0 reward, emits JSONL in OpenAI SFT/trl shape
- Schema: {messages: [{role, content}], reward: float in [-1,1], meta: {story_id, model, tokens, duration, ...}}
- CLI: uv run python -m lib.export_trajectories --out trajectories.jsonl [--tsv results.tsv]
- Created tests/test_export_trajectories.py with 11 tests covering reward mapping, schema validation, CLI output
- Added Trajectory Export section to README.md

### Patterns discovered
- results.tsv status is "keep" (pass) or "reject" (fail) — not "pass"/"fail"
- Worker logs (.spiral/_claude_raw_<pid>.tmp) are named by PID with no direct story_id link; synthetic messages from TSV metadata work well for SFT/RL training
- lib/export_trajectories.py can be imported as module or run as __main__ via python -m lib.export_trajectories

### Gotchas
- Pre-existing mypy errors in reachability_verifier.py, routing/ucb1_select.py, ui/spiral_dashboard.py are NOT caused by this story
- cache_read_tokens + cache_creation_tokens = total tokens (review_tokens is separate)
## Iteration 2 - Mon, Apr 27, 2026 12:56:40 AM
Completed: Trajectory export: results.tsv + worker logs to JSONL for LLM fine-tuning / RL training (ID: US-1264) in 8m


## Iteration 3 - Story: Phase V: Integrate evidence aggregator (US-1246)

### What was implemented
- Updated prd.schema.json: verification.testFiles now accepts string OR array of strings via oneOf
- Created lib/phase_v_evidence.py: minimal evidence aggregator that reads prd.json and writes {story_id: {target: PASS|FAIL}} to .spiral/evidence.json
- Wired evidence aggregator into lib/phases/phase_v_validate.sh (called before write_checkpoint)
- Created tests/test_phase_v_evidence.py with 4 tests covering aggregate, empty, file write, and error cases

### Patterns discovered
- Previous attempt failed with 3209-line diff — keeping implementation minimal (53+47 lines for new files) is critical
- prd.schema.json verification.testFiles was already array-only; oneOf pattern cleanly supports both string and array

### Gotchas
- Pre-existing mypy errors in other files; not caused by this story
- Must use encoding='utf-8' for all file opens on Windows
